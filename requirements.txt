# dlib removed from pip requirements — install separately (see notes below)
# dlib removed from pip requirements — install separately (see notes below)
numpy==2.2.6
# opencv-python removed — install via conda (conda-forge)
setuptools>=78.1.1