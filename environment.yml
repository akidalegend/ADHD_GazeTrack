name: GazeTracking
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy=2.2.6
  - opencv=4.10           # install opencv via conda
  - pip
  - setuptools>=78.1.1
  - pip:
    # opencv-python removed to avoid pip/conda conflict
# dlib intentionally omitted — install separately (see notes)
# ...existing code...